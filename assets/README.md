# assets

用于放置静态资源（图片、图标、二维码、未来 Demo 截图等）。

- 可把 **WeChat 二维码** 存为例如 `assets/wechat-qr.png`，再在 `index.html` 的联系区引用。
- 案例演示页面上线后，可在此存放封面图或示意图。

部署到 GitHub Pages 时，此目录会随仓库一并发布；在站内使用相对路径即可，例如：`assets/wechat-qr.png`。
